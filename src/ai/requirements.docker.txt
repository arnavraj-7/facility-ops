# Curated runtime deps for the AI dispatch microservice (Docker / production).
# The sibling requirements.txt is a full local `pip freeze` and is NOT
# installable on Linux (contains Windows-only and ML packages). Versions here
# match what the service is developed against.
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
python-dotenv==1.2.2
langgraph==1.2.1
langgraph-checkpoint==4.1.1
langchain-core==1.4.0
langchain-google-genai==4.2.3
